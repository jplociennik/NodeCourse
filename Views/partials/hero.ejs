<!-- Bootstrap Hero Section with Green Theme -->
<div class="bg-success text-white py-3">
    <div class="container text-center">
        <div class="d-flex align-items-center justify-content-center mb-2">
            <img src="/img/NodeLogo.png" alt="Logo" class="hero-logo me-3" style="height: 120px; width: auto;">
            <h1 class="h2 fw-bold mb-0">
                <%= typeof title !== 'undefined' ? title : 'Witaj na mojej stronie' %>
            </h1>
        </div>
        <p class="mb-2">
            <%= typeof subtitle !== 'undefined' ? subtitle : 'Przykładowy layout HTML' %>
        </p>
    </div>
</div> 